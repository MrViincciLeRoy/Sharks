<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Action to trigger manual import -->
    <record id="action_fetch_gmail_statements" model="ir.actions.server">
        <field name="name">? Import from Gmail</field>
        <field name="model_id" ref="model_email_statement"/>
        <field name="state">code</field>
        <field name="code">
result = model.fetch_statements_from_gmail()
        </field>
    </record>

    <!-- Tree view with import button -->
    <record id="view_email_statement_tree" model="ir.ui.view">
        <field name="name">email.statement.tree</field>
        <field name="model">email.statement</field>
        <field name="arch" type="xml">
            <tree string="Email Statements">
                <header>
                    <button name="%(action_fetch_gmail_statements)d" 
                            type="action" 
                            string="? Import from Gmail" 
                            class="btn-primary"/>
                </header>
                <field name="date"/>
                <field name="name"/>
                <field name="sender"/>
                <field name="bank_name"/>
                <field name="transaction_count"/>
                <field name="has_pdf" invisible="1"/>
                <field name="state" widget="badge" 
                       decoration-info="state=='draft'" 
                       decoration-success="state=='parsed'"/>
            </tree>
        </field>
    </record>

    <!-- Form view with PDF download -->
    <record id="view_email_statement_form" model="ir.ui.view">
        <field name="name">email.statement.form</field>
        <field name="model">email.statement</field>
        <field name="arch" type="xml">
            <form string="Email Statement">
                <header>
                    <button name="action_download_and_parse_pdf" 
                            string="? Download &amp; Parse PDF" 
                            type="object" 
                            class="oe_highlight" 
                            invisible="state != 'draft'"/>
                    <field name="state" widget="statusbar"/>
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="%(action_bank_transaction)d" type="action" 
                                class="oe_stat_button" icon="fa-list-ul" 
                                context="{'default_statement_id': id}">
                            <field name="transaction_count" widget="statinfo" 
                                   string="Transactions"/>
                        </button>
                        <button class="oe_stat_button" icon="fa-file-pdf-o" 
                                name="action_view_attachments" type="object">
                            <field name="attachment_count" widget="statinfo" 
                                   string="PDFs"/>
                        </button>
                    </div>
                    <div class="oe_title">
                        <h1>
                            <field name="name" readonly="1"/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="sender"/>
                            <field name="bank_name"/>
                            <field name="date"/>
                        </group>
                        <group>
                            <field name="gmail_id"/>
                            <field name="has_pdf" widget="boolean_toggle"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Transactions">
                            <field name="transaction_ids">
                                <tree>
                                    <field name="date"/>
                                    <field name="description"/>
                                    <field name="transaction_type"/>
                                    <field name="amount"/>
                                    <field name="state"/>
                                </tree>
                            </field>
                        </page>
                        <page string="HTML Content" invisible="not body_html">
                            <field name="body_html" widget="html"/>
                        </page>
                        <page string="Text Content">
                            <field name="body_text"/>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Add method to view attachments -->
    <record id="action_email_statement" model="ir.actions.act_window">
        <field name="name">Email Statements</field>
        <field name="res_model">email.statement</field>
        <field name="view_mode">tree,form</field>
    </record>

    <menuitem id="menu_email_statement" name="Email Statements" 
              parent="menu_statement_importer_root" action="action_email_statement" sequence="20"/>
</odoo>