<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Auto-generate forecasts daily -->
        <record id="cron_generate_forecasts" model="ir.cron">
            <field name="name">Generate Expense Forecasts</field>
            <field name="model_id" ref="model_expense_forecaster"/>
            <field name="state">code</field>
            <field name="code">
forecasters = model.search([('auto_update', '=', True)])
for forecaster in forecasters:
    forecaster.generate_forecasts()
            </field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="numbercall">-1</field>
            <field name="active">True</field>
        </record>

        <!-- Auto-generate cashflow projections weekly -->
        <record id="cron_generate_projections" model="ir.cron">
            <field name="name">Generate Cashflow Projections</field>
            <field name="model_id" ref="model_cashflow_projection"/>
            <field name="state">code</field>
            <field name="code">model.generate_projections(months=6)</field>
            <field name="interval_number">7</field>
            <field name="interval_type">days</field>
            <field name="numbercall">-1</field>
            <field name="active">True</field>
        </record>

        <!-- Monthly analytics report -->
        <record id="cron_monthly_analytics" model="ir.cron">
            <field name="name">Generate Monthly Analytics</field>
            <field name="model_id" ref="model_expense_analytics"/>
            <field name="state">code</field>
            <field name="code">model.create_monthly_analysis()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">months</field>
            <field name="numbercall">-1</field>
            <field name="active">True</field>
        </record>
    </data>
</odoo>